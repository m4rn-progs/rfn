use crossterm::terminal;
use std::fs;
use std::io;

enum Mode {
    Sun,
    Moon,
    Cast,
    Sky,
    Star,
}

enum Actions {
    MoveUp,
    MoveDown,
    MoveLeft,
    MoveRight,
    Backspace,
    Return,
    Esc,
    CtrlC,
    CtrlV,
}

fn main() {
    
}
